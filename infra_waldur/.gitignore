# Local variable overrides (contain secrets)
*.local.tfvars

# Terraform working directory
.terraform/
.terraform.lock.hcl

crash.log
*.tfvars

# State contains the ssh private key, the Waldur token and the neo4j password
terraform.tfstate
terraform.tfstate.*

*.pem
